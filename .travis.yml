dist: xenial
language: python
python:
  - "3.7"

services:
  - docker

branches:
  only:
  - master
  
install:
  - pip install -r requirements.txt
  - pip install codecov

script: 
  - make build
  - bash docker-push.sh

after-success:
  - codecov